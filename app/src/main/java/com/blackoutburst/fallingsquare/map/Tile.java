package com.blackoutburst.fallingsquare.map;

import com.blackoutburst.fallingsquare.graphics.Colors;

public class Tile extends Tiles {
    public Tile(String texture, int x, int y, int width, int height, boolean solid, Colors color, int rotation) {
        super(texture, x, y, width, height, solid, color, rotation);
    }

}